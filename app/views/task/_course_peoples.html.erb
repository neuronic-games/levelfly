<% @people.each do |p| %>
<div class="task_member" id="task_people_div_<%=p.profile.id%>">
  <input type="checkbox" name="chk_task_people[]" checked="true" value="<%=p.profile.id%>" id="<%=p.profile.id%>" />

  <img src="<%=p.profile.image_file_name%>" profile_id="<%=p.profile_id%>" class="profile avatar_medium"/>

   <%= p.profile.full_name %>
</div>
<% end %>

<script type="text/javascript">
<!--
  $($('input[name="chk_task_people[]"]')).each(function(){
    if(this.checked) {
      setStorage("task_people_"+$(this).attr('value'), $(this).val());
    } else {
      deleteStorage("task_people_"+$(this).attr('value'));
    }
  });

-->
</script>