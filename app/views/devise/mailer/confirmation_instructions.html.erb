<p>Welcome <%= @resource.full_name || @resource.email %>!</p>

<p>You can confirm your account through the link below:</p>

<p>
  <a href="<%= url_for :controller => 'confirmations', :action => 'show', :confirmation_token => @resource.confirmation_token, :only_path => false %>">Confirm my account</a>
</p>
