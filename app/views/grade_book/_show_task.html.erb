<%if !@latest_course.nil? %>

<span class="fakebox">All Task</span>
<select id="task_id">
  <% unless @tasks.nil? %>
    <% @tasks.each do |t| %> 
    <option value="t_<%= t.id %>"><%= t.name %></option>
    <% end %>
  <% end %>
</select>

<%end%>